# Dr. Solution. — منصتك الأكاديمية الذكية 🎓

## إيه هي Dr. Solution؟

**Dr. Solution.** منصة أكاديمية مفتوحة المصدر تحول كتبك، أبحاثك، محاضراتك، وملفات PDF إلى تجربة دراسة متكاملة. كل صفحة تتحول لسلايد بصري مع شرح، ترجمة، تحليل للمحتوى، وصف للصور، وخلاصة، وأسئلة اختبار.

---

## ماذا تنتجها المنصة؟

| المخرجات | الوصف |
|---------|-------|
| **شرح كل صفحة** | مع صورة الصفحة الأصلية، شرح تعليمي عميق، ترجمة دقيقة |
| **تحليل الرسوم** | تفسير الجداول، المخططات، الأشكال التوضيحية |
| **نقاط رئيسية** | خلاصة 3-7 نقاط لكل سلايد |
| **أهداف تعلم** | خلاصة شاملة للمادة + أهداف قيّمة |
| **قاموس مصطلحات** | مصطلحات إنجليزية مع شرح عربي |
| **أسئلة اختبار** | متعدد الخيارات، صح/خطأ، إجابة قصيرة، مقالية |
| **ملف HTML** | تفاعلي، يعمل بدون إنترنت بعد التحميل |

يدعم أساليب مخصصة للمحاضرات، الأبحاث، الكتب، والمستندات الجامعية.

---

## تحليل فيديو YouTube 🎬

حوّل فيديوهات يوتيوب إلى شرح منظم:

1. **الترجمة أولاً**: يحاول يقرأ ترجمة YouTube (عربي/إنجليزي)
2. **Whisper كخط دوم**: لو مافي ترجمة، يستخدم `faster-whisper` محلياً (نموذج `tiny`)
3. **تقسيم ذكي**: يستخدم فصول يوتيوب (chapters) للتقسيم، أو يقسم كل 45 ثانية
4. **استخراج الإطارات**: يستخدم YouTube Thumbnails API (سريع، بدون تحميل الفيديو!)
5. **تحليل Gemini**: كل مقطع يُرسل (صورة + نص) لـ Gemini
6. **تركيب نهائي**: خلاصة، أهداف، قاموس، وخطة دراسية

### المواصفات الحالية:
- **أقصى مدة فيديو**: ساعة واحدة
- **أقصى مقاطع**: 12 مقطع لكل فيديو
- **أقصى فيديوهات في قائمة**: 3
- **Whisper**: للفيديوهات حتى 30 دقيقة
- **نموذج Whisper**: `tiny` (خفيف على الذاكرة)

---

## كيف يعمل؟

```
الملف PDF / رابط فيديو
    ↓
استخراج النص (PDF) أو الترجمة (YouTube)
    ↓
تقسيم لسلايدات / مقاطع
    ↓
كل سلايدة ← Gemini (صورة + نص)
    ↓
تجميع النتائج ← Gemini (خلاصة + أهداف + قاموس + خطة دراسية)
    ↓
HTML تفاعلي + JSON + خطة دراسية
```

### مزودات الذكاء الاصطناعي المدعومة:

| المحرك | vision | الملاحظات |
|--------|--------|----------|
| **Gemini 3.1 Flash-Lite** | ✅ | الافتراضي المجاني، يدعم النص والصور |
| **Claude 3.5 Sonnet** | ✅ | مدفوع، دقة عالية |
| **DeepSeek Chat** | ❌ | مدفوع، نص فقط |

---

## التشغيل المحلي

```powershell
py -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
Copy-Item .env.example .env
python run.py
```

افتح `http://127.0.0.1:8000`

### الإعدادات المهمة في `.env`:

```env
GEMINI_API_KEY=AIza...           # مفتاح Google AI Studio (مجاني)
GEMINI_MODEL=gemini-3.1-flash-lite
REQUIRE_USER_API_KEY=false       # false = يستخدم مفتاح السيرفر
MAX_PAGES=24                     # أقصى صفحات للتحليل
VIDEO_MAX_DURATION_SECONDS=7200  # أقصى مدة فيديو (ساعة)
VIDEO_MAX_SEGMENTS=12            # أقصى مقاطع فيديو
WHISPER_MODEL=tiny               # نموذج Whisper (خفيف)
```

---

## المميزات المتقدمة

### 1. كاش للفيديوهات
ما يعيد تحليل نفس الفيديو — النتائج محفوظة لمدة ساعة.

### 2. تقسيم حسب فصول يوتيوب
يستخدم الفصول الرسمية من يوتيوب بدلاً من التقسيم العشوائي.

### 3. خطة دراسية منظمة
يدعم مستويات بلوم (تذكر/فهم/تطبيق/تحليل/تقييم/إبداع) ويحدد:
- ترتيب المواضيع منطقياً
- وقت دراسة كل هدف
- المتطلبات السابقة لكل هدف

### 4. استخراج الإطارات الذكي
- **YouTube Thumbnails API** (أولوية): سريع، موثوق، بدون تحميل
- **ffmpeg streaming** (fallback): يحمّل بس الإطار المطلوب من رابط البث

### 5. BYOK (Bring Your Own Key)
- المفتاح يُرسل عبر HTTPS مع كل طلب
- يُستخدم في الذاكرة ثم يُهمل
- لا يُحفظ في السجل أو الكوكيز أو ملف النتيجة

---

## النشر على Render

1. ارفع المشروع على GitHub
2. أنشئ **New Blueprint** على Render
3. سيقرأ `render.yaml` ويشغّل تلقائياً
4. كل مستخدم يضيف مفتاح Gemini من الواجهة

```
GET /api/health → {"status":"ok","version":"3.2.0","providers_ready":2}
```

---

## التطوير والاختبار

```powershell
pip install -r requirements-dev.txt
pytest
```

---

## الهيكل التقني

```
app/
├── api/routes.py           # API endpoints (analyze, chat, video)
├── core/config.py          # الإعدادات والمتغيرات السرية
├── services/
│   ├── documents.py        # PDF → صفحات + صور
│   ├── providers.py        # AI providers (Gemini, Claude, DeepSeek)
│   └── video.py            # ترجمة + تقسيم + تحليل الفيديو
├── models.py               # Pydantic models
static/
├── index.html              # الواجهة الرئيسية
├── css/app.css             # التصميم
├── js/app.js               # المنطق (رفع، تحليل، اختبار)
├── about.html              # صفحة "عن المنصة"
└── privacy.html            # سياسة الخصوصية
```

---

## المستقبل القريب

- [ ] بطاقات Anki (تصدير APKG)
- [ ] تصدير SCORM / Moodle XML
- [ ] مقارنة فيديوهين
- [ ] فهرسة وبحث في الأهداف
- [ ] OCR محلي احتياطي (Tesseract)

---

## روابط مفيدة

- **الكود المصدري**: GitHub
- **النسخة الحية**: https://dr-solution.onrender.com
- **Google AI Studio**: https://aistudio.google.com/api-keys
- **الوثائق**: README.md

---

**Dr. Solution.** — رفيقك الأكاديمي الذكي.
حوّل المحتوى إلى فهم، لا مجرد نسخ.
